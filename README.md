# land-dashboard

부동산 매물 대시보드. 이 저장소는 **자동 생성**되며 `gh-pages` 브랜치의 `index.html` + `data.js`
두 파일만 GitHub Pages 로 서비스한다. 데이터는 비공개 수집 저장소의 GitHub Actions 가 매일 갱신한다.

- 서비스 URL: `https://windeln.github.io/land-dashboard/`
- 브랜치: `gh-pages` (발행 대상)

> 수동으로 편집하지 말 것 — 발행 시 덮어써진다.
