# zipda.github.io

부동산 매물 대시보드. 이 저장소는 **자동 생성**되며 `gh-pages` 브랜치의 `index.html` + `data.js`
두 파일만 GitHub Pages(조직 사이트)로 서비스한다. 데이터는 비공개 수집 저장소의 로컬 스크립트
(`deploy.sh`)가 갱신한다.

- 서비스 URL: <https://zipda.github.io/>
- 발행 브랜치: `gh-pages`

> 수동으로 편집하지 말 것 — 발행 시 덮어써진다.
